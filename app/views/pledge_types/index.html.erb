<h1>Listing pledge_types</h1>

<table>
  <tr>
    <th>Project</th>
    <th>Name</th>
    <th>Desc</th>
    <th>Image</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pledge_types.each do |pledge_type| %>
  <tr>
    <td><%= pledge_type.project_id %></td>
    <td><%= pledge_type.name %></td>
    <td><%= pledge_type.desc %></td>
    <td><%= pledge_type.image %></td>
    <td><%= link_to 'Show', pledge_type %></td>
    <td><%= link_to 'Edit', edit_pledge_type_path(pledge_type) %></td>
    <td><%= link_to 'Destroy', pledge_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Pledge type', new_pledge_type_path %>
